<?hh // strict

namespace HackPack\HackMini;
use HackPack\HackMini\Middleware\Manager;

function registerWebMiddleware(Manager $manager) : void
{
    // Call $manager->add() to add any middleware you like
}

function registerCliMiddleware(Manager $manager) : void
{
    // Call $manager->add() to add any middleware you like
}
